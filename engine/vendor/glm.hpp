#pragma once

// Ensure that everything glm does takes radians as input
#define GLM_FORCE_RADIANS
// Ensure things are alligned by default
#define GLM_FORCE_DEFAULT_ALIGNED_GENTYPES
#include <glm/glm.hpp>
#include <glm/gtc/matrix_transform.hpp>
